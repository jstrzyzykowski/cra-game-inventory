import React from 'react';
import '../styles/Item.css';

const Item = props => {
    return (
        <div className="item-outer">
            {/* {props.item.name} */}
        </div>
    );
}

export default Item;